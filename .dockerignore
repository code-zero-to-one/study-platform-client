# Git / local metadata
.git
.gitignore
.github
.claude
.codex
.vscode
.husky

# Local dependencies and build artifacts
node_modules
.next
out
storybook-static
coverage
playwright-report
test-results

# Logs and caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.turbo
.cache
.DS_Store

# Local-only env files. Keep `.env` in the Docker context because CI creates it
# before `docker build` and the Dockerfiles copy it into the build/runtime image.
.env.local
.env.*.local
